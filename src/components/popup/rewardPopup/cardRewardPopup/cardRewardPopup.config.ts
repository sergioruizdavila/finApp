/**
* config()
* @description - Card Reward Popup config file
*/

(function() {
    'use strict';

    angular
        .module('finApp.components.rewardPopup.faCardPopup', [])
        .config(config);

    function config() {
        console.log('faCardPopup config launched');
    }
})();
